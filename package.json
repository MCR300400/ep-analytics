{
  "name": "ep-analytics",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply ep-analytics-db --local",
    "db:remote": "wrangler d1 migrations apply ep-analytics-db --remote",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.7.0"
  }
}
